/**
 * Mesh SDK helpers – wallet + transaction utilities.
 * Install @meshsdk/core when ready: npm i @meshsdk/core
 */

export async function getAvailableWallets(): Promise<string[]> {
  // TODO: return (await import("@meshsdk/core")).BrowserWallet.getInstalledWallets();
  return [];
}
